fn take(
    a: i32,
    b: i32,
    c: i32,
    d: i32,
    e: i32,
    f: i32,
    value: &str,
    tail: f32
) -> f32 {
    return tail;
}

fn main() -> i32 {
    let result = take(1, 2, 3, 4, 5, 6, "hello", 1.5);
    return 0;
}
